[project]
name = "concierge-audio"
version = "0.1.0"
description = "MiniMax and BytePlus text-to-speech studio for Unitree G1"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1,<4",
    "python-dotenv>=1.0,<2",
    "requests>=2.32,<3",
]

[tool.uv]
package = false
